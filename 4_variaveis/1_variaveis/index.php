<?php

// $ na frente do nome das variavies

// pode criar uma variavel por meio do nome de outra com valor diferente = $$
    //ex: $x = "teste";
        // $$x = 5; - novo valor de $x

// pode criar variavel com referencia a outra - =&

// ESCOPO

    // local - variavel declarada em uma função;
    // global - variavel declarada fora de uma função (usar global);
    // static - variavel declarada em uma função, porém seu valor permanece salvo (usar static);
    // parametros de funcao - variavel passadas em uma função podendo ser utilizadas ao longo da mesma;

// OPERADORES
    // . concatenacao 

    // COMPARACAO
        // == igualdade
        // === identico
        // != diferente
        // !== não identico
        // > e >= maior
        // < e <= menor

    // LOGICOS
        // && and
        // || or
        // ! not

    // (CAST)
        // ex: $a = (float) "5.32" - string convertida para float
    
    // ATRIBUIÇÃO
        // = atribuir valor a variavel
        // +=, -=, /=, *=, %=
    
    // TERNARIO
        // ex: 5 > 2 ? echo "5 é maior que 2" : echo "5 é menor que 2"
    