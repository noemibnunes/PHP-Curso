<?php

// date('d/m/y') - dia, mês e ano (formatar data atual)

// date('D, M - Y') - dia(nome), mês(nome) e ano (formatar data atual)

// date('d/m/y', strtotime('+2 years')) - 2 anos a mais

// mktime(hora, minuto, segundo, mes, dia, ano) - a partir disso gera uma data

// DateTime - tratar a data como objeto
    // format - formata a data
    // modify - altera a data

// setDate - recebe ano, mes e dia alterando a data 

// setTime - recebe hora, minuto e segundo alterando o tempo da data

// diff - diferenca entre duas datas

// date_default_timezone_set - alterar o fuso horario